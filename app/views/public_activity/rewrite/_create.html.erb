<li id=<%= activity.trackable_id %>>

  <p><%= link_to User.find(activity.owner_id).name, User.find(activity.owner_id), class: "user" %> <%= rewrite_action_word %> a rewrite called <%= link_to activity.parameters[:rewrite_title], rewrite_path(activity.trackable_id), class: "rewrite-title" %> based on the following snippet: <%= link_to activity.parameters[:snippet_content], snippet_path(activity.trackable_id), class: "snippet" %></p>

  <p class="feed-item-timestamp"><small><%= time_ago_in_words(activity.created_at) %> ago</small></p>

</li>
