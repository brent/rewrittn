<%= form_for(current_user.relationships.find_by(followed_id: @target.id, followed_type: @target.class.to_s), html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unstar", class: "btn unstar-btn" %>
<% end %>

