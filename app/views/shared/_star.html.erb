<%= form_for(current_user.relationships.build(followed_id: @target.id, followed_type:@target.class.to_s ), remote: true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <div><%= f.hidden_field :followed_type %></div>
  <%= f.submit "Star", class: "btn star-btn" %>
<% end %>
