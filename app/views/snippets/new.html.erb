<% provide(:title, "Create a snippet") %>

<%= form_for @snippet do |f| %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.label :source %>
  <%= f.text_field :source %>

  <%= f.button "Add snippet", class: "btn" %>
<% end %>
