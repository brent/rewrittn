<% provide(:title, @title) %>
<div class="user-info">
  <p><%= @user.name %></p>
  <%= render 'shared/stats' %>
</div>

<h3><%= @title %></h3>
<% if @users.any? %>
  <ul class="users">
    <%= render @users %>
  </ul>
  <%= will_paginate %>
<% end %>
